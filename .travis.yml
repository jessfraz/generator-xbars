language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-xbars' ]; then cd .. && eval "mv $currentfolder generator-xbars" && cd generator-xbars; fi

